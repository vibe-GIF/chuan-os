name: assistant_overlay
description: "jarvis特效"


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.10.4



dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  mesh_gradient: ^1.3.8
  window_manager: ^0.5.1
  flutter_acrylic: ^1.1.4
  system_info2: ^4.1.0
  battery_plus: ^7.0.0
  connectivity_plus: ^7.1.1
  network_info_plus: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  msix: ^3.16.13

msix_config:
  display_name: Assistants-Overlay
  publisher_display_name: rubintry
  identity_name: com.jarvis.assistant-overlay
  msix_version: 1.0.0.0
  logo_path: images/logo.png
  trim_logo: false
  capabilities: internetClient, location, microphone, webcam
  languages: en-us, zh-cn
  enable_at_startup: false
  store: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/ico-jarvis.png
    - assets/jarvis/
    - assets/ironman/

  shaders:
    - shaders/metaballs_shader.frag
    - shaders/orb_shader.frag
#    - shaders/siri_loader.frag

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
